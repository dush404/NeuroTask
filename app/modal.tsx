// Unused Expo template modal — replaced by ai-chat.tsx in NeuroTask.
// This file is kept to satisfy Expo Router's route expectations if cached.
import { Redirect } from "expo-router";
export default function Modal() {
  return <Redirect href="/(tabs)" />;
}
